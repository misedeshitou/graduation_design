\chapter{路径规划仿真试验}\label{chap:path_planning_simulation}
本章节仿真效果很多基于动画展示，不便于在论文中直接展示，相关仿真视频与源代码已上传至GitHub项目主页（\url{https://github.com/misedeshitou/ColorDynamic}）。

\section{仿真环境介绍}
本毕业设计的路径规划训练与验证采用轻量级开源强化学习环境 Sparrow 构建（开源项目原址：\url{https://github.com/XinJingHao/Sparrow-V2}）。该环境的设计理念为“麻雀虽小，五脏俱全”，不仅底层架构轻量高效，同时涵盖了丰富的物理和感知模拟。

Sparrow 基于 Pygame 框架构建，主要模拟了一个配备二维激光雷达（2D Lidar）的差速双轮/阿克曼转向式机器人，在富含动态移动与静态随机障碍物的二维平面中执行点到点（Point-to-Point）的无碰撞导航任务。机器人的主要交互框架定义如下：
\begin{itemize}
    \item \textbf{状态观测空间（Observation）}：系统输入包含传感器返回的几十条雷达离散测距点数据、机器人自身当前的线速度与角速度以及目标点相对于车体坐标系的距离和航向偏移角。
    \item \textbf{动作输出空间（Action）}：由智能体输出连续或离散的控制指令，直接映射为机器人的目标线速度和转向角速度。
    \item \textbf{任务目标（Objective）}：训练一个具备强泛化能力的智能体（Agent），在高度不确定的复杂动态随机环境中成功规避移动障碍物，以优化的路径和最短的时间安全抵达终点。
\end{itemize}

选择 Sparrow 作为本课题核心仿真平台的考量主要集中在仿真效率与高度还原的物理逻辑两个方面：

\subsection{极具优势的仿真效率与泛化能力}
\begin{enumerate}
    \item \textbf{原生支持张量向量化（Vectorization）}：有别于传统采用循环单步运行的环境，Sparrow 完全基于 PyTorch 构建了底层逻辑。借助可自定义的超参数 $N$，环境允许同时拉起成百上千个平行的独立副本地图。所有的运动学积分、雷达模拟和碰撞检测都以矩阵运算的形式在 GPU 上并行处理，这使得强化学习算法的数据采样效率呈指数级跃升。
    \item \textbf{端到端的高效数据流通（Data Flow）}：通常的物理引擎（如 Gazebo、Webots）需要在 CPU 上完成运算，再将观测结果拷贝传输至 GPU 供神经网络进行前向推理，这带来了巨大的通信开销。而 Sparrow 直接在显存内部生成深度网络所需的 Tensor 观测张量，实现了计算节点的无缝衔接。
    \item \textbf{高度的域随机化机制（Domain Randomization）}：为了跨越从仿真应用到现实物理控制的差距（Sim2Real），环境源码被注入了极强的随机化逻辑。每轮任务重置时，环境会动态且随机地初始化障碍物的数量、形状、散布及其不规则移动速度；同时还支持在雷达扫描和底盘执行中施加高斯白噪声与控制迟滞。这种复杂性赋予了所训练出的大模型出色的抗扰动泛化能力。
\end{enumerate}

\subsection{贴近底层的物理仿真逻辑}
\begin{enumerate}
    \item \textbf{惯性运动学模型模拟（Kinematics）}：该环境摒弃了指令即时响应的理想化设定，内置了一阶低通滤波器来模拟现实世界电机和轮式的物理迟滞与惯性特性。每一次状态更新的实际输出速度 $V_{\text{now}}$ 被描述为：
    \begin{equation}
        V_{\text{now}} = K \cdot V_{\text{previous}} + (1-K) \cdot V_{\text{target}}
    \end{equation}
    这说明即便输出了较大的刹车/加速控制动作 $V_{\text{target}}$，车体速度也不会发生瞬间跃变，有效倒逼算法需提前规划预留安全距离，高度吻合真实电缆机器人的电驱响应特征。
    \item \textbf{像素级 GPU 并行雷达扫描（Lidar Scan）}：传统的二维避障常常调用耗时的计算几何库来求交点。Sparrow 巧妙地采用栅格/像素级编码来映射障碍物并模拟射线。环境会将雷达中心作为原点，利用高度并行的“烟花式”探照算法，通过张量加速同时向四周扩散计算数百条射线的首个触阻距离。这极大程度上平替并复刻了真实的激光雷达扫描应用情景。
\end{enumerate}
为了极致还原真实环境中的控制延迟与电机响应物理特性，本毕业设计在实际训练中，将上述运动学滤波器系数特别设定为 $K=0.6$。这意味着当前的理想动作指令起初只有一部分会立即生效，剩余的速度增量需要逐渐克服旋转惯性，从而倒逼智能体自主学习平滑控制与提前预判。

\subsection{动作空间与执行逻辑}
针对不同的算法框架，该仿真环境支持智能体以离散（Discrete）或连续（Continuous）两种形式进行动作输出以实现路径规划。

\subsubsection{离散动作空间选择}
在离散控制模式下，动作空间被映射为 8 个预设的控制指令。这一映射表对应了机器人在不同线速度与角速度下的运动基元（见表 \ref{tab:discrete_action}）。这种设定适合如 DQN 等无法直接输出连续动作的传统强化学习算法。

\begin{table}[H]
    \centering
    \caption{离散动作索引及其对应的物理运动含义}
    \label{tab:discrete_action}
    \begin{tabular}{llcc}
        \toprule
        \textbf{动作索引} & \textbf{对应物理含义} & \textbf{向前方线速度 ($v$)} & \textbf{转向角速度 ($\omega$)} \\
        \midrule
        0 & 左前转（小半径） & $0.2 \times v_{\max}$ & $+ \omega_{\max}$ \\
        1 & 左前转（大半径） & $v_{\max}$ & $+ \omega_{\max}$ \\
        2 & 直线前进 & $v_{\max}$ & $0$ \\
        3 & 右前转（大半径） & $v_{\max}$ & $- \omega_{\max}$ \\
        4 & 右前转（小半径） & $0.2 \times v_{\max}$ & $- \omega_{\max}$ \\
        5 & 直线后退 & $- v_{\max}$ & $0$ \\
        6 & 减速 / 微速前进 & $0.1 \times v_{\max}$ & $0$ \\
        7 & 原地静止 & $0.0$ & $0.0$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{连续动作空间选择}
当采用能够处理连续动作空间的先进算法（如 TransSAC、SAC 等）时，智能体策略网络（Actor）将输出一个二维动作向量 $[a_1, a_2]$。通常该向量的值域被限制在 $[-1, 1]$ 之间。环境会在底层通过预设的最大速度比例尺（Continuous Scale），将该网络输出连续投影至实际底盘物理量：
\begin{itemize}
    \item \textbf{实际线速度}：$V_l = a_1 \times 50 \text{ cm/s}$
    \item \textbf{实际角速度}：$\omega = a_2 \times 2 \text{ rad/s}$
\end{itemize}

\subsection{DWA算法实现}
\subsection{强化学习算法实现}
\subsection{特征状态与奖励函数设计}
为了让强化学习模型在动态避障中做出最合理的决策，环境的状态构建与奖励指引发挥着决定性作用。

\subsubsection{动作状态叠加与延迟处理}
真实的机器人系统从指令下发到底盘电机输出必然存在滞后响应。为此，环境采用了一种“动作历史叠加（Action State Stacking）”的高级特征融合机制。在底层系统生成观测向量时，不仅会采集物理反馈，还会提取并拼接“动作历史序列”：
\begin{itemize}
    \item \textbf{预期动作片段（Current Action）}：智能体在当前网络步计算并下发的预期动作指令。
    \item \textbf{真实动作片段（Real Action）}：受前文 $K=0.6$ 滤波平滑影响与控制延迟后，此时车体历史真正在执行的滞后动作。
\end{itemize}
将预期与历史的执行时差喂给观测空间，是为了强制强化学习算法产生自我意识，领悟出“当前的移动态势是此前几步命令共同滞后作用的结果”，有效避免策略盲目修正所引发的车体“画龙”（剧烈高频S型抖动）现象，大幅度提升了整体导航的平滑性。

\subsubsection{综合状态空间构成（Observation Space）}
经过全局归一化（Normalize）处理后，智能体每一个交互步所能“看到”并摄入神经网络的完整状态向量囊括：
\begin{itemize}
    \item \textbf{目标相对距离（D2T）}：当前机器人中心坐标距终点区域的欧氏距离向量。
    \item \textbf{环境相对偏角（Alpha）}：目标点相对于机器人当前车头朝向的夹角偏差，特征极值非线性归一化到 $[-1, 1]$ 范围。
    \item \textbf{自身运动速度（Velocities）}：包含当前的平移线速度和转向角速度。
    \item \textbf{激光雷达数据（Lidar Data）}：经过底层网格扫描下采样分组后实时映射出的雷达测距阵列，借此勾勒外界静/动态障碍轮廓特征。
    \item \textbf{动作历史队列（Action History）}：前述用以辅助消除控制时差的“预期-真实”动作动作列表记录。
\end{itemize}

\subsubsection{复合引导奖励函数（Reward Function）}
为促使训练网络快速在探索期收敛出最优控制策略，模拟器内置了多维度的精细奖惩数学核算：
\begin{itemize}
    \item \textbf{进度奖励（Progress）}：计算前后状态帧到目标距离的欧氏范数差，智能体越逼近目标即获正向积累，反之背离航向即遭惩罚抵扣。
    \item \textbf{方向引导（Direction）}：依据观测到的方位夹角 $\alpha$，鼓励智能体持续拨正车体朝向并锁定终点视野。
    \item \textbf{速度偏好倾向（Velocity）}：激励模型在空旷平坦路段果断输出最高速度前进特性（如在离散中偏爱动作 2），而对无实际避撞意图的持续倒车或龟速前行进行微小数值的扣除。
    \item \textbf{生存判决与死亡重罚（Terminal）}：仿真任务的核心判别。一旦本体越入目标限定区域便视为通关，单回合派发巨额引导奖励（$+200$）；相反，只要雷达射线检测到机器人与任何静态金具、移动障碍发生干涉碰撞，或驶离给定围挡，都会即刻被系统判定死亡坠机，施以最高额度的负分惩罚（$-200$）并强行重启当前场景。
\end{itemize}
\subsection{DWA算法实现}
\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{figures/showcase.png}
  \caption{Sparrow环境下的路径规划仿真展示}
  \label{fig:sparrow-showcase}
\end{figure}
\subsection{SAC算法实现}
\subsection{SAC串联transformer网络架构}
\subsection{路径规划算法对比}
采用100次独立随机初始化的测试场景，分别评估了 DWA、SAC 和 TransSAC 三种算法在路径规划任务中的表现。评测指标包括：

\texttt{ep\_steps}：平均每回合步数

先在 \texttt{vectorized\_dwa\_evaluation()} 里统计 \texttt{total\_steps / finished}，见 \texttt{evaluate\_and\_plot.py:107-158}。
其中到达目标用真实步数，失败/超时按 500 次计罚，见 \texttt{evaluate\_and\_plot.py:138-143}。

\texttt{ep\_r}：平均每回合总奖励

统计方式是 \texttt{total\_r / finished}，见 \texttt{evaluate\_and\_plot.py:145-158}。

\texttt{arrival\_rate}：到达率

单次评估里是 \texttt{arrived\_vec.sum() / opt.N}，见 \texttt{evaluate\_and\_plot.py:151-158}。
外层再对 C 次 reset 求平均，见 \texttt{evaluate\_and\_plot.py:66-76}。
\texttt{normed\_total\_score}：综合分（最终排名）

步数分：$\texttt{normed\_ep\_steps} = (500-\texttt{ep\_steps})/(500-70)$
奖励分：$\texttt{normed\_ep\_r} = \texttt{ep\_r}/220$
综合分：$\texttt{normed\_total\_score} = (\texttt{normed\_ep\_steps} + \texttt{normed\_ep\_r} + \texttt{arrival\_rate})/3$
代码见 \texttt{evaluate\_and\_plot.py:82-86}。
并且会写到 TensorBoard 的是 \texttt{ep\_steps}、\texttt{ep\_r}、\texttt{arrival\_rate}，见 \texttt{evaluate\_and\_plot.py:78-80}。

对于DWA经典算法，测试结果如下：
Index          |Total Score    |Arrival Score  |Step Score     |Reward Score   
DWA            |0.304          |0.53           |0.442          |-0.06          

对于强化学习算法，不同训练轮数的模型表现如下：

\begin{itemize}
    \item \textbf{SAC（Soft Actor-Critic）}：在训练初期（约 10 万步）表现较为不稳定，平均每回合步数较多，
    奖励较低，到达率约为 30\%。随着训练的推进，SAC 的表现逐渐提升，在 50 万步后平均每回合步数
    \item \textbf{TransSAC（Transformer-enhanced SAC）}：引入 Transformer 结构后，模型在训练初期就
    展现出较好的性能，平均每回合步数较少，奖励较高，到达率约为 50\%。随着训练的推进，TransSAC 的表现持续提
    升，在 50 万步后平均每回合步数进一步减少，奖励显著增加，到达率超过 70\%。
\end{itemize}
与传统的 DWA 算法相比，SAC 和 TransSAC 在路径规划上无论是从平均每回合步数、奖励还是到达率方面都表现出明显的优势。
尤其是 TransSAC，通过引入 Transformer 结构，显著提升了模型的泛化能力和决策效率，在复杂动态环境中的表现尤为突出。

对于DWA经典算法，相关参数调优如下：
对于一个以恒定线速度 $v$ 和角速度 $\omega$ 运动的对象，其位置随时间 $t$ 的变化可写为：
\begin{equation}
\begin{aligned}
x(t) &= x_0 + \int_{0}^{t} v \cos(\theta_0 + \omega\tau) \, \mathrm{d}\tau
= x_0 + \frac{v}{\omega}\left[\sin(\theta_0 + \omega t) - \sin(\theta_0)\right],\\
y(t) &= y_0 + \int_{0}^{t} v \sin(\theta_0 + \omega\tau) \, \mathrm{d}\tau
= y_0 - \frac{v}{\omega}\left[\cos(\theta_0 + \omega t) - \cos(\theta_0)\right].
\end{aligned}
\end{equation}

\paragraph{A. 原始离散运动模型（Euler Method）}
这是最基础的更新方式，假设在 $\Delta t$ 时间内速度和朝向恒定。
\begin{equation}
\begin{cases}
x_{k+1} = x_k + v_k \cos(\theta_k) \Delta t \\
y_{k+1} = y_k + v_k \sin(\theta_k) \Delta t \\
\theta_{k+1} = \theta_k + \omega_k \Delta t
\end{cases}
\end{equation}

\paragraph{B. 改进后的积分运动模型（Exact Integration）}
该模型考虑了在 $\Delta t$ 时间内朝向角 $\theta$ 的连续变化（即圆弧运动）。
\begin{equation}
\begin{cases}
x(t) = x_0 + \frac{v}{\omega}(\sin(\theta_0 + \omega t) - \sin(\theta_0)) \\
y(t) = y_0 - \frac{v}{\omega}(\cos(\theta_0 + \omega t) - \cos(\theta_0)) \\
\theta(t) = \theta_0 + \omega t
\end{cases}
\end{equation}
\paragraph{改进模型的优势}
在论文论述中可从以下三个维度展开：
\begin{enumerate}
    \item \textbf{消除轨迹线性化误差}：原始离散模型默认采样周期内沿直线运动；当角速度较大或采样频率较低时，圆弧会被折线近似，从而产生几何误差。积分模型给出运动学方程的解析解，可显著降低该误差。
    \item \textbf{提高定位精度与稳定性}：在里程计（Odometry）与惯性导航中，误差会随时间累积。积分模型能降低单步预测误差，尤其在高速转弯或大角度偏转工况下可提供更准确位姿估计，从而提升滤波稳定性。
    \item \textbf{增强低频采样鲁棒性}：工程实践中采样频率可能受限（如 $10\,\mathrm{Hz}$）。相较高度依赖高频采样的离散模型，积分模型在较大时间步长 $\Delta t$ 下仍能保持较高轨迹精度。
\end{enumerate}

\subsubsection{障碍物代价函数}
\subsubsection{指数代价函数 (Soft Constraint)}

该模型采用负指数函数计算非碰撞区域的代价值，其数学表达式为：

\begin{equation}
Cost = e^{-\frac{d}{\sigma}}
\end{equation}

其中，$d$ 表示物体与障碍物之间的净距离（$net\_dist$），$\sigma$ 为平滑因子，通常取安全距离（$safe\_dist$）的三分之一。

\paragraph{逻辑设计与优势：}
\begin{itemize}
    \item \textbf{远距离衰减：} 当 $d$ 较大时，$Cost$ 趋于 $0$，表示环境对路径规划的影响极小。
    \item \textbf{近距离惩罚：} 随着 $d$ 的减小，$Cost$ 以指数级迅速增长，形成非线性惩罚。
    \item \textbf{边界平滑性：} 选取 $\sigma = \frac{safe\_dist}{3}$ 使得在 $d = safe\_dist$ 处，$Cost = e^{-3} \approx 0.049$。这种参数设计为
    机器人提供了一个平滑的避障过渡带，使其能够平滑地绕开障碍物，避免了硬约束带来的运动不连续性。
\end{itemize}

\section{DWA算法调试与局部极小值问题分析}

在动态窗口法（Dynamic Window Approach, DWA）算法的实测与部署过程中，机器人初期表现出明显的“原地打圈”现象，且设定的碰撞急停机制未能按预期生效。通过记录与分析算法运行状态，发现该现象主要源于代价函数（Cost Function）设计的不平衡以及参数配置的局部最优陷阱。

\subsection{典型异常现象：“打圈”陷阱分析}

当机器人陷入“打圈”状态时，其在评估所有可行轨迹后，认为原地旋转或远离障碍物绕圈的综合代价最低。导致这一现象的核心矛盾点可归结为以下三个方面：

\begin{itemize}
    \item \textbf{权重压制与避障过度主导：} 在前期的代价权重分配中，避障代价（Obstacle Cost）的“排斥力”远大于目标点代价（Goal Cost）的“吸引力”。只要前往目标点的路径上存在微小的碰撞风险，系统便会为了追求极低的避障代价，而放弃尝试穿过狭小空间，最终选择在原地保持较高转速（以满足速度评价指标），从而陷入局部最优。
    
    \item \textbf{长预测时域（Predict Time）的负面效应：} 在初期配置中，轨迹预测时间被设定为 $5.1\text{ s}$。在最高 $50\text{ cm/s}$ 的线速度下，系统会评估未来长达 $2.5\text{ m}$ 的运动轨迹。在密集的障碍环境中，如此长的预测轨迹在转弯时产生的“扫掠面积”极大，极易与障碍物发生干涉。为使这 $5.1\text{ s}$ 的轨迹不触碰任何障碍物，系统被迫选择曲率极大（即原地打转）的极短安全轨迹。
    
    \item \textbf{航向角代价（Heading Cost）的缺失考量：} 原始算法评价指标仅依赖轨迹末端至目标点的欧氏距离。当机器人背离目标时，为使终点靠近目标，系统可能生成倒车或大半径圆弧轨迹。由于缺乏对机身朝向目标点的显式约束，机器人在旋转过程中失去了强指向性，进一步加剧了原地绕圈的发生概率。
\end{itemize}

\subsection{动态避障策略与参数调优}

针对上述局部极小值与动态避障失效问题，经过多轮实车与仿真调试，本文对DWA算法进行了如下优化：

\subsubsection{预测时间与灵敏度的权衡}
将预测时间由 $5.1\text{ s}$ 大幅下调至 $1.0\text{ s}$，并同步降低了碰撞判断的距离阈值。该调整有效减小了轨迹的扫掠面积，提高了系统对环境的响应灵敏度。需要指出的是，预测时间不宜过小，否则会导致系统“短视”，仅在临近碰撞时才产生高代价值，从而因制动距离不足而发生实际碰撞。

\subsubsection{动静态障碍物的差异化处理机制}
实验结果表明，传统的“遇障即停”逻辑在面对动态障碍物时表现不佳，极易导致机器人陷入停滞。为此，系统舍弃了全局急停机制，转而要求机器人在动态环境中保持连续的运动能力以实现主动规避。

针对静态障碍物，为提高机器人的谨慎程度，本文采用了一种在临近区域内强制拉高代价值的硬约束策略。具体而言，当障碍物净距离 $net\_dist$ 小于等于 $15.0\text{ cm}$ 时，赋予其极高的惩罚值：
\begin{equation}
norm\_obs\_cost[net\_dist \le 15.0] = 5.0
\end{equation}
在调试中发现，若该硬性惩罚值设置过高（即梯度过大），会导致各条可行轨迹的代价差异被抹平，机器人更易倾向于停滞。因此，本文将该区域惩罚值设定为与总代价函数最大值相当的水平，且仅对静态障碍物生效，从而在保证安全性的同时维持了路径规划的平滑性。此外，考虑到引入航向角代价会大幅增加参数空间的耦合度与调优复杂度，本系统最终舍弃了该指标。

\subsection{最终参数配置}

经过上述策略的优化，机器人成功实现了鲁棒的动态避障功能。最终确定的DWA核心运动学限制与代价权重参数如表 \ref{tab:dwa_params} 所示。

\begin{table}[htbp]
    \centering
    \caption{DWA算法核心参数优化配置表}
    \label{tab:dwa_params}
    \begin{tabular}{llc}
        \toprule
        \textbf{参数变量名} & \textbf{物理意义} & \textbf{设定值} \\
        \midrule
        $max\_speed$ & 最大线速度 & $50.0 \text{ cm/s}$ \\
        $min\_speed$ & 最小线速度 & $-50.0 \text{ cm/s}$ \\
        $max\_yawrate$ & 最大角速度 & $2.0 \text{ rad/s}$ \\
        $max\_accel$ & 最大线加速度 & $2.0 \text{ m/s}^2$ \\
        $max\_dyawrate$ & 最大角加速度 & $2.0 \text{ rad/s}^2$ \\
        $v\_reso$ & 线速度搜索分辨率 & $0.1 \text{ m/s}$ \\
        $yawrate\_reso$ & 角速度搜索分辨率 & $0.2 \text{ rad/s}$ \\
        $dt$ & 控制采样周期 & $0.1 \text{ s}$ \\
        $predict\_time$ & 轨迹预测时域 & $1.0 \text{ s}$ \\
        $goal\_cost\_gain$ & 目标距离代价权重 & $0.6$ \\
        $speed\_cost\_gain$ & 期望速度代价权重 & $0.1$ \\
        $obstacle\_cost\_gain$ & 障碍物距离代价权重 & $0.3$ \\
        $robot\_radius$ & 机器人膨胀半径 & $5.0 \text{ cm}$ \\
        $safe\_dist$ & 避障预警半径 & $20.0 \text{ cm}$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{强化学习算法}
\subsubsection{TransSAC算法}